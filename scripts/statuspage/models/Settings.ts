/*
 * Copyright (c) 2021 by HerrTxbias.
 *
 * Using / Editing this without my consent is not allowed.
 */

interface Settings {
    spio: {
        pageID: string
        apiKey: string
    }
    sp: {
        url: string
        apiKey: string
    }
}

export = Settings;
