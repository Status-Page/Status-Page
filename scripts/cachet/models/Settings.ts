/*
 * Copyright (c) 2021 by HerrTxbias.
 *
 * Using / Editing this without my consent is not allowed.
 */

interface Settings {
    cachet: {
        domain: string
        apiKey: string
    }
    sp: {
        url: string
        apiKey: string
    }
}

export = Settings;
